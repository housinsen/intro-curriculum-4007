extends layout

block content
  h1= title
  p Welcome to #{title}
  if user
    p Hello, #{user.username}
    a(href="/logout") Logout
  else
    a(href="/login") Login
  div(id="block", class="block")
  button#scaling-buttonS 拡大縮小
  button#moving-button 移動
  script(src="/javascripts/bundle.js")